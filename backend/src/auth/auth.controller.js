import { Controller } from '@nestjs/common';

@Controller('auth')
export class AuthController {}


//porque no se push en el otro computador tengo que terminar el auth controller tengo que presentalo en 15 dias y no he echo nada 